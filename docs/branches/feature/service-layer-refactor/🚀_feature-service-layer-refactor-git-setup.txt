# ============================================================
# 🚀 FEATURE BRANCH SETUP (SERVICE LAYER REFACTOR)
# ============================================================

# 🔹 Step 1: Switch to main branch
git checkout main

# 🔹 Step 2: Pull latest changes from remote
git pull

# 🔹 Step 3: Create new feature branch
git checkout -b feature/service-layer-refactor

# 🔹 Step 4: Push branch to remote (initial push)
# This sets upstream tracking so future pushes are simple
git push -u origin feature/service-layer-refactor


# ============================================================
# 🔍 VERIFY BRANCH
# ============================================================

# Check current branch
git branch

# Check remote tracking
git branch -vv


# ============================================================
# 📌 NOTES
# ============================================================

# - Work only in this feature branch
# - Do NOT commit directly to main
# - Push changes regularly
# - After completion → create PR → merge to main


# ============================================================
# 🚀 NEXT STEPS
# ============================================================

# 1. Add service layer (cart, checkout, order)
# 2. Update routes to use services
# 3. Test locally (cart → checkout → orders)
# 4. Commit changes
# 5. Push to feature branch

# ========================================================================
# 🚀 ADD + COMMIT + PUSH (CART SERVICE LAYER) Timely Pushing from feature
# ========================================================================

# Stage all changes (services + updated routes)
git add .

# Commit with clear message
git commit -m "Refactor: added service layer (cart, checkout, order) without API changess"

# Push to feature branch
git push

git status

# ============================================================
# 🔹 STEP 1: Ensure you're on feature branch Merging flow
# ============================================================
git checkout feature/service-layer-refactor

# 🔹 Make sure everything is committed
git status

# If any changes:
git add .
git commit -m "Final: stable cart & order flow verifiedd"

# 🔹 Push latest feature branch changes
git push origin feature/service-layer-refactor


# ============================================================
# 🔹 STEP 2: Switch to main branch
# ============================================================
git checkout main

# Pull latest main (IMPORTANT)
git pull origin main


# ============================================================
# 🔹 STEP 3: Merge feature → main
# ============================================================
git merge feature/service-layer-refactor

# If no conflicts → auto merge ✅
# If conflicts → fix → git add . → git commit


# ============================================================
# 🔹 STEP 4: Push main to remote
# ============================================================
git push origin main